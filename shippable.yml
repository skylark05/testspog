jobs:
  - name: j1
    type: runSh
    steps:
      - TASK:
          script:
            - echo "hello, world"
  - name: j2
    type: runSh
    steps:
      - IN: j1
      - IN: testspog_master
        switch: off
      - TASK:
          runtime:
            options:
              env:
                - IMG_VER_NAME: "v6.5.1"
                - IMG_VER_NAME_DASH: "v6-5-1"
          script:
            - echo "hello, world"
            - pushd $(shipctl get_resource_state testspog_master)
            - cat foo.txt
            - shipctl replace foo.txt
            - cat foo.txt
            - popd
